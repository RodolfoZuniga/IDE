main {
    int x, y;
    float z;
    bool flag;
    
    cin >> x;
    y = 5;
    z = 3.14;
    
    if x < y then
        cout << "x is less than y";
        flag = true;
    else
        cout << "x is not less than y";
        flag = false;
    end
    
    while x > 0
        x = x - 1;
        cout << x;
    end
    
    do
        y = y + 1;
        cout << y;
    while y < 10;
}